[project]
name = "datatools"
version = "0.1.0"
description = "Simple data analysis tools"
authors = [
    {name = "Your Name"}
]
requires-python = ">=3.8"

dependencies = [
    "numpy>=1.24"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"